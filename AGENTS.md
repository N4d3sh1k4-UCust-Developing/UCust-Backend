# Project: UCust

## Architecture
Microservices on Spring Boot 4 / Java 25:
- **api-gateway** (`:8100`) — Spring Cloud Gateway (WebFlux), routes: /api/auth/** → security-service, /api/users/** → user-service, /api/business/** → business-service
- **security-service** (`:8101`) — auth (JWT + OAuth2 Yandex), registration, tokens, rate limiting (Bucket4j). JWT access-токен содержит claims: `sub` (userId), `roles`, `city` (определяется по IP через `UserGeoService` → ip-api.com, best-effort, никогда не блокирует логин; локальные адреса возвращают null → claim отсутствует). Все точки входа (login/refresh/yandex-mobile/link-social/OAuth2) кладут `city` в токен.
- **user-service** (`:8102`) — user profiles, avatars (MinIO)
- **business-service** (`:8104`) — project/business management, logos (MinIO)
- **notification-service** (`:8103`) — email (RabbitMQ consumer + Spring Mail + Thymeleaf)
- **billing-service** (`:8106`) — billing/billing_quota management
- **generative-orchestration-service** (`:8107`) — generative AI orchestration (calls billing-service quota; external AI через Единый Шлюз Оркестратора `POST /api/v1/orchestrator/execute` + callback `POST /api/v1/ai/callback` с `X-Internal-Secret`)
- **configuration-service** (no port) — Spring Cloud Config (currently disabled)
- **Prod-Nginx (rinhbaby, НЕ в git, правится вручную)**: внешний stream nginx (конфиг `/etc/nginx/conf/nginx.conf` + `/etc/nginx/conf/streams/xray-module.conf`) — SNI-роутер по `ssl_preread` на :443, без TLS-терминации, проксирует TCP на внутренние nginx (:8444–8452, конфиги в `/etc/nginx/conf/sites-enabled/`), которые терминациируют TLS и проксируют на docker-контейнеры. **PROXY protocol полная цепочка (2026-09-12)**: внешний сервер с белым IP (5.42.124.217, WireGuard peer, WG `10.0.0.0/24`) шлёт PROXY protocol на rinhbaby :443 (`listen ... proxy_protocol`); stream-контекст nginx на rinhbaby теперь умеет realip: `set_real_ip_from 127.0.0.1; set_real_ip_from 10.0.0.0/24;` (+ было пересобрано `nginx` из исходников в `/opt/build/nginx-1.29.5` с добавлением `--with-stream_realip_module`; старый бинарь сохранён `nginx.bak-20260912-171638`; сборка нужна была потому что `real_ip_header` **не** существует в stream-контексте, модуль stream_realip автоматически берёт реальный IP из PP-заголовка). Далее `proxy_protocol on;` в stream-server + `proxy_protocol` в каждом внутреннем `listen` + `set_real_ip_from 127.0.0.1; real_ip_header proxy_protocol;` в http-контексте → реальный IP клиента доходит до внутренних nginx и уходит в `X-Real-IP`/`X-Forwarded-For`. Без этого на L4-хопе IP терялся (`10.0.0.1` = WG-адрес внешнего сервера). Эндпоинты `api.ucust` = внутренний nginx on :8446 → gateway :8100. Reload: `sudo systemctl reload nginx`. Бэкапы конфигов: `/etc/nginx/backup-*` и `/etc/nginx/sbin/nginx.bak-*`. SSH: `ssh n4d3sh1k4@rinhbaby`.
- **ClientIpUtils (security-service)**: единый резолв реального IP запроса — `X-Forwarded-For` (первый в списке) → `X-Real-IP` → `getRemoteAddr()`. Используется в `AuthController` (login/refresh/yandex-mobile/link-social) и `OAuth2SuccessHandler` для claim `city` и логов сессий.
- **common** — shared lib (ApiResponse, GlobalExceptionHandler, BaseException). `GlobalResponseHandler` оборачивает в ApiResponse все ответы, **кроме** `/internal/**` (raw-body S2S-контракт для сервис-ту-сервис, напр. `/internal/quota/*` для QuotaClient).

## Key Changes Made in This Session

### 1. Eureka removed
- All `eureka-client` dependencies removed from build.gradle
- All `eureka.*` config blocks removed from all services
- `netflix-eureka` module deleted from project, settings.gradle, .gitmodules, git
- API Gateway uses static routes (container names / localhost)

### 2. Ports changed
- `8180→8100`, `8181→8101`, `8182→8102`, `8183→8103`, `8184→8104`

### 3. MySQL → PostgreSQL
- `mysql-connector-j` → `postgresql:42.7.5` in all build.gradle
- JDBC URLs: `jdbc:postgresql://...`
- `init.sql` → PostgreSQL syntax
- PostgreSQL image `postgres:16`, host port `5440`

### 4. Configs converted .properties → .yml
- All services now use `application.yml` + `application-prod.yml`
- Default credentials via `${VAR:default}` in dev, strict `${VAR}` in prod
- `.env` file at root (in .gitignore) for local secrets
- Dev defaults use simple passwords without `$` (Docker Compose interprets `$` as variable)

### 5. OPS repo created (`ucust-ops/`)
- Separate stack: PostgreSQL, RabbitMQ, MinIO
- Shared network: `ucust-net`
- Full RabbitMQ topology in `definitions.json` (exchange + 2 queues + 4 bindings)
- MinIO auto-init: creates `user-service` bucket
- Scripts: backup.sh, restore.sh

### 6. CI/CD updated
- Build: 7 services (api-gateway, security, user, business, notification, billing, generative-orchestration; Dockerfile + application-prod.yml created for billing/generative)
- Deploy: SCP docker-compose.yml → SSH pull + up -d
- Secrets passed via `envs` (DB_PASSWORD, JWT_SECRET_ACCESS, SERVER_TAILSCALE_IP, ..., AI_SERVICE_URI)

### 7. Docker network
- Infrastructure (`ucust-ops`) creates `ucust-net` (driver: bridge)
- App stack (`docker-compose.yml`) uses `ucust-net` as external

### 8. Database creation
- `ops/postgres/init.sql` создаёт БД только при первом старте пустого volume
- Новая БД добавляется в `ops/scripts/ensure-databases.sh` (идемпотентно создаёт недостающие при каждом деплое ops)

### 9. AI: Единый Шлюз Оркестратора (v2.5.0)
- **Внешний контур**: AI на отдельном сервере, связь через WireGuard (`10.0.0.2` → `10.0.0.1`).
- **Бэкенд → AI**: `POST {AI_ORCHESTRATOR_ENDPOINT}/api/v1/orchestrator/execute`, envelope `{task_type, user_id, session_id, payload, sync_backend}`, заголовок `X-Internal-Secret` (общий секрет `INTERNAL_API_SECRET`). `brand_id`/`tenant_id` кладётся **внутрь payload**.
- **AI → бэкенд**: Auto-Push на `POST /api/v1/ai/callback` (при `sync_backend: true`), тоже с `X-Internal-Secret`. Роут в gateway **публичный** (`/api/v1/ai/**`), проверка секрета в `AiCallbackController`. Callback URL для AI-ноды: `http://10.0.0.1:8100/api/v1/ai/callback`.
- **generative-orchestration-service**: `OrchestratorClient` (execute), `AiCallbackService` (generate_post → Post), `AiGenerationService` (проверка квоты `post` + инкремент), `PostFactory`. Эндпоинты: `POST /orchestration/ai/execute` (generic passthrough), `POST /orchestration/generate` (с квотой). Перед генерацией `AiGenerationService` запрашивает проект (`BusinessServiceClient` → business-service `GET /projects/{id}` с заголовками `X-User-Id`/`X-User-Roles`, best-effort) и обогащает payload полями бренда (company_name, niche, description, city, target_audience, tone_of_voice, website/instagram/telegram, logo_url, business_hours; значения из запроса имеют приоритет, `BUSINESS_SERVICE_URI` задаётся на клиенте) + структурированными flat-ключами из `brandProfile` проекта (usp ← positioning, key_benefits ← swot.strengths, brand_goals ← goals, products_catalog ← services); `brand_profile` в payload строкой НЕ отправляется (риск SecurityGuard шлюза) — вместо этого `RagClient` best-effort индексирует брендбук в RAG AI-контура (`POST /api/v1/ai/rag/ingest`, doc_id=`brand_profile:{projectId}`, X-Internal-Secret, SimpleClientHttpRequestFactory).
- **Посты**: `POST /orchestration/posts/{id}/confirm`, `POST /orchestration/posts/{id}/publish`, `GET /orchestration/posts/{id}`, `GET /orchestration/projects/{projectId}/posts`, `PATCH /orchestration/posts/{id}` (partial-обновление text/imageUrl/hashtags/targetPlatforms/scheduledAt), `POST /orchestration/posts/{id}/ai-review` (AI-аудит поста через `CriticClient` → контур `/api/v1/ai/critic/review` (критик Чарли Мангера): передаются текст поста + `industry` проекта как niche, возвращается `CriticReviewResponse {status, critic, data, error}`, правки применяются на клиенте через PATCH). Все операции принимают `Authentication` (principal = userId string) и проверяют владельца: пост, `userId` которого не совпадает, отдаётся как 404 (`ContentNotFoundException`). Список постов — только по `findByProjectIdAndUserIdOrderByCreatedAtDesc`. `@EnableScheduling` — на `GenerativeOrchestrationServiceApplication`. `PostFactory` толерантен к форме ответа Шлюза: `generated_post {telegram_html → vk_text, media{kind,src,palette}, hashtags[]}` (реальная структура контура) либо flat-конвенция (`post_text_html`/`post_text`/`image_url`); `media.src` → `imageUrl`, массивы склеиваются через пробел. `telegram_buttons`/`security_passed`/`title` в `posts` не персистятся (нет колонок).
- **Async-генерация** (`POST /orchestration/generate/async`, `GET /orchestration/tasks/{id}`): `AsyncGenerationService` НЕ использует удалённый `AIServiceClient` — submitAsync дергает Единый Шлюз через `OrchestratorClient.execute` (`task_type=generate_post`, `session_id=task.id`, `sync_backend=true`), обогащает payload данными проекта (`BusinessServiceClient.findProject`, best-effort) + индексирует брендбук в RAG (`ragClient`). Если sync-ответ Шлюза содержит текст — посты создаются сразу (задача `COMPLETED`, `resultPostId`); иначе задача `PROCESSING`, результат ждём пушем на `/api/v1/ai/callback` (`AiCallbackService` идемпотентен: задача `COMPLETED`/`FAILED` → callback пропускается). `checkTask` — чистое чтение БД. `TaskStatusResponse` содержит `generatedText` и alias `postId` (фронт по ним достаёт текст готового поста). Ошибка Шлюза → задача `FAILED`.
- **Фотографии в async-генерации (2026-09-22)**: `POST /orchestration/generate/async` принимает multipart (`@RequestPart("request")` JSON `GenerateRequest` + `@RequestPart("files")` MultipartFile[]) — тот же сервис/роут, что и JSON-версия. `AsyncGenerationService.submitAsyncWithMedia(request, files, userId)` загружает файлы через `MediaStorageService.uploadProjectFiles` в MinIO (best-effort, try/catch → при ошибке `attachments=[]`, генерация без фото) и кладёт `payload.attachments = [{name, url}]` (пустой список НЕ кладётся). Внутренний `submit(request, attachments, userId)` — общий для обоих входов. Мок парит контуру: `get_mock_post_result` при attachments возвращает блок `moondream_analysis` (count, image_urls, brand_colors, mood_tags, ocr_detected_text, description, visual_context_for_llm) + добавляет в `post_text` фразу «На снимке — …». Тесты: `AsyncGenerationServiceTest` (upload → attachments в `TaskRequest.payload` по ArgumentCaptor; upload-фейл → генерация без attachments), мок (`generate_post с attachments → moondream_analysis.count==N и image_urls`). Фронт: `Attachment` (`lib/dashboard/attachments.ts`) теперь хранит `file: File`; `generateAsyncWithMedia(req, files)` (`lib/api/orchestration.ts`) шлёт FormData (`request` = Blob application/json, `files` = файлы); `CreateView.runGeneration` — если `photos.items` содержат файлы, уходит multipart-запрос, иначе прежний JSON (`generateAsync`); `generateBody`-fallback (сервер недоступен) по-прежнему имитирует фото одной фразой. **E2E через мок на :8000**: generate_post с attachments → moondream_analysis.count=2, urls из payload, `.data.post_text` заканчивается фразой про снимок.
- **Планирование и «отложить»**: `POST /orchestration/posts/{id}/schedule` (body `{scheduledAt}` ISO-8601) ставит `SCHEDULED`+`scheduledAt`; крон `PostPublishScheduler` каждые 30с переводит `SCHEDULED`+`scheduledAt<=now` → `PUBLISHED`+`publishedAt` (реальной отправки в соцсети нет). `POST /orchestration/posts/{id}/reject` → `REJECTED` («отложить»). Фронт: `schedulePost(id, scheduledAt)` (`combineDateTime` в поясе Europe/Moscow, offset +03:00), `rejectPost(id)` — кнопка «Отложить» в `PostEditView`.
- **RAG-БД**: PostgreSQL 16 + pgvector (+ HNSW) **локально на AI-сервере** (`ai_smm`, postgres/postgres) — бэкенд напрямую не подключается, только `rag_ingest`/`rag_query` через шлюз.
- **Медиа в постах (шаги А–В спеки)**: `POST /orchestration/posts/generate` (multipart: часть `request` = JSON `AiGenerateRequest`, часть `files` = бинарники) — файлы сохраняются в MinIO (`io.minio:minio:9.0.0`, бакет `generative-orchestration`, ключ `media/{projectId}/{filename}` с sanitize от path-traversal, бакет ensure best-effort), в payload оркестратора уходят как `attachments: [{name, url}]` (визуальный контекст для Moondream VQA + SaigaLLM на стороне Шлюза; сам бэкенд картинок не читает). Публичный доступ к файлам — через gateway-роут `/s3/**` (br `minio-s3-public`, публичный, stripPrefix(1), target `minio.s3.uri`; в prod `MINIO_S3_URI` = `http://minio:9000`, НЕ `localhost:9020`), т.е. URL вида `https://api.ucust.n4d3sh1k4.site/s3/generative-orchestration/media/...`. Конфиг: `minio.endpoint/access-key/secret-key/bucket/public-base-url` (dev `http://127.0.0.1:9020/generative-orchestration`, prod `MINIO_*` strict; `MINIO_PUBLIC_BASE_URL` должен быть доступен AI-контуру на 10.0.0.2, напр. `https://api.ucust.n4d3sh1k4.site/s3/generative-orchestration`). `MediaStorageService` → `List<Attachment>`, `AiGenerateRequest.attachments` теперь `List<Attachment>`, пустой список = поведение как раньше (`generate`). **Статус медиа (e2e 2026-09-10, после hotfix на ноде):** в стоковом развёрнутом Шлюзе v2.5.0 (`/opt/ucust/ai` на `185.182.108.212`) `moondream_vqa._to_pil_image` **не скачивал URL-аттачи** (в модуле нет HTTP-импортов; url трактовался как локальный путь → `count:0`, `image_url:null`). Сервер-нода ходит в интернет через прокси `194.67.95.7:3127` (`no_proxy` содержит CIDR `10.0.0.0/8`, который **питоновский urllib не понимает**, в отличие от curl — поэтому urllib слал WG/MinIO-URL через прокси и зависал). **Хотфикс применён на ноде** (`skills/moondream_vqa.py`, бэкап `*.bak-20260909`): в `_to_pil_image` добавлено скачивание `http(s)` сначала напрямую (`ProxyHandler({})`), затем fallback через env-прокси (по 2 попытки? нет, по 1 кандидату), gateway перезапущен `venv/bin/python api_gateway.py` (nohup, порт 8000). После хотфикса `generate_post` с attachment-URL реально анализирует фото: `moondream_analysis.count:1`, палитра, slot_mapping/fusion — проверено и на WG-URL `10.0.0.3:9020`, и на local-URL. **НО** `image_url`/`photo_url` возвращаются `null` (`photo_gen_seconds:null`) даже с `generate_image:true` — генерация фото (ComfyUI :8188) в флоу `generate_post` для аттачей не запускается (контурная доработка, не интеграция бэка). Внимание для прод-медиа: `https://api.ucust.n4d3sh1k4.site/s3/...` из-под прокси ноды недоступен (curl 000) → контуру нужен reachable источник медиа (напр. `http://10.0.0.1:8100/s3/...` через WG или whitelist на прокси), иначе даже после хотфикса прод-attachments не скачаются.
- **Каскадное удаление проекта (2026-09-10)**: `DELETE /api/business/projects/{id}` → `ProjectService.delete` (business-service) чистит строку + логотип в MinIO (best-effort, `projects/{projectId}/logo_...` в бакете user-service) и публикует `project.deleted {projectId, userId}` на `user-exchange` (метод `ProjectEventPublisher.projectDeleted`, событие `ProjectDeletedEvent` в common). generative-orchestration подписан на `project-deletion-queue` → `ProjectDeletedEventHandler` → `ProjectDeletionService` (атомарно в 1 tx): `postRepository.deleteByProjectIdAndUserId`, `generationTaskRepository.deleteByProjectId`, `businessAnalysisRepository.deleteByProjectId` + `MediaStorageService.deleteProjectMedia` (листинг `media/{projectId}/...` рекурсивно + removeObject, best-effort по объекту). Очередь/DLQ объявлены в `RabbitProjectConfig` (routing key `project.deleted`, DLQ `project-deletion-queue.dlq` через `user-exchange.dlx`). **Ограничение:** RAG-документы контура (`brand_profile:{projectId}`, `channel_context:{projectId}`) не удаляются — у развёрнутого Шлюза v2.5.0 нет эндпоинта удаления из RAG-БД.
- **Анализ бизнеса (онбординг)**: `POST /orchestration/analysis` (gateway `/api/v0/orchestration/analysis`) принимает `{companyName?, url?, notes?, documents? (string[] data-URL)}`, `BusinessAnalysisService.start()` синхронно дергает Шлюз (`task_type` выбирается: `quick_scan` при url / `analyze_documents` при только документах / `onboard_user` иначе, `payload{company_name, urls, documents, raw_notes, images:[], fast_mode:true}`, `sync_backend:true`), на ошибку/таймаут помечает `FAILED` без падения. Callback-пуш контура (`quick_scan`/`analyze_documents`/`onboard_user`/`onboarding`, session_id = наш `sessionId`) → `BusinessAnalysisService.complete()` (идемпотентно), результат нормализуется `BusinessAnalysisMapper` (толерантный, aliases) в `profile` формы BrandProfile + `ProjectPatch` и best-effort применяется через PATCH `/projects/{id}` (`BusinessServiceClient.updateProject`, X-User-Id/X-User-Roles). Сущность `business_analysis`. Фронт: `lib/api/analysis.ts` → fallback на ручной режим при `FAILED`/недоступном AI. `BusinessAnalysisMapper` дополнительно понимает сырую выдачу `WebsiteCollector` из контура (`meta.title/description`, `og_image`, `headings`, `paragraphs`, `extracted_links` → website/instagram/telegram, `full_extracted_text` как запасное описание) — профиль/патч собираются, даже если Шлюз вернёт необработанный парс сайта. **Парсинг документов (e2e 2026-09-10, hotfix на ноде):** в стоковом v2.5.0 `analyze_documents` ждал локальные пути (data-URL студентов → `os.path.exists` → «Файл не найден»), а в venv отсутствовали `pypdf`/`python-docx`/`python-pptx` (`No module named 'pypdf'`), работал только txt/md/csv. Хотфикс `collectors/document_collector.py` (бэкап `*.bak-20260909`): `extract_text_from_file` декодирует data-URL (`data:mime;base64,…` → временный файл с расширением по mime), три библиотеки установлены в venv, gateway перезапущен. Проверено живьём: data-URL PDF/DOCX → `extracted_documents` + `indexed_in_rag_count`. **Маппер v2 (2026-09-10):** разворачивает вложенные структуры ответа контура — `prefilled_profile` (business_name→company_name, niche→field, city, social_links), `raw_sources.website` (описание из meta.description/key_texts/structured_dossier, соцсети из social_links), `extracted_documents` (тексты документов → описание); «Позиционирование» при отсутствии `usp`/`positioning` у контура подставляется из описания сайта/документа (≤280 симв., поле «О проекте» на `/onboarding/review`). Фронт: устаревший баннер «ИИ-анализ скоро появится» заменён на «Профиль предзаполнен ИИ-анализом». **Ограничения:** 1) при `url`+`documents` в одном запросе `BusinessAnalysisService.chooseTask` выбирает `quick_scan`, который документы игнорирует (тестировать раздельно); 2) нишу контур определяет только в quick_scan → в analyze_documents поле «Сфера деятельности» пустое; 3) авто-пуш контура в `.env` ноды указывает `JAVA_BACKEND_CALLBACK_URL=http://10.0.0.1:8080/api/v1/ai/callback` (старый порт; прод-бэкенды на 8100) — автопуш в прод не долетает, но результат приходит синхронно в `response.data()`.
- **Верификация соцсетей (онбординг, этап A)**: `POST /orchestration/socials/verify` (gateway `/api/v0/orchestration/socials/verify`) принимает `{channel, reference}` → `SocialVerifyResponse {channel, reference, verified, provider, error}`. `SocialVerificationService` (best-effort, SSRF-защита: DNS-резолв, отклоняются приватные/локальные адреса): telegram — Bot API `getChat` при `TELEGRAM_BOT_TOKEN` (иначе проверка публичной страницы `t.me`, provider=`t.me`: t.me отвечает 2xx даже для несуществующего хэндла («Contact @…» без виджета), поэтому существующий канал распознаётся по маркерам `tgme_page_title`/title «Telegram: View @…»; запрос идёт с `Accept-Encoding: identity`, т.к. JDK HttpClient сам просит gzip, но не распаковывает); vk/ok/max/instagram/zen/rutube/avito — HTTP reachability 2xx/3xx; остальные (whatsapp и т.п.) → `UNSUPPORTED_CHANNEL`. `verified=true` означает лишь «страница/канал существует», права на публикацию дадут только реальные коннекторы (этап B: Telegram Bot API без OAuth; VK/OK OAuth2 с зарегистрированными приложениями; Instagram — FB Graph API; Zen без публичного API). Конфиг: `services.socials.telegram-bot-token: ${TELEGRAM_BOT_TOKEN:}` + `services.socials.telegram-bot-proxy: ${TELEGRAM_BOT_PROXY:}` (опционально) (опциональный). `TELEGRAM_BOT_PROXY` — HTTP-пул: записи `host:port` | `http://host:port` | `user:pass@host:port` | `host:port:user:pass` через запятую/перевод строки, ротация round-robin (`parseProxies`/`roundRobinSelector`, клиент JDK `HttpClient` с authenticator; прокси идёт только на запросы верификации, остальной трафик сервиса не затрагивает). **Прод-туннель (2026-09-22)**: на rinhbaby в compose добавлен сервис `xray` (образ `teddysun/xray:latest`, контейнер `xray-ucust`, интерфейс ucust-net, HTTP-inbound :1080) — локальный VLESS-клиент подписки MagnumVPN (нода `Messengers`, api.ferrin.uk:443; конфиг с UUID правится вручную на сервере: `/home/n4d3sh1k4/projects/ucust/xray/config.json`, в git НЕ попадает). `TELEGRAM_BOT_PROXY=http://xray:1080` (дефолт в compose/application-prod) — gen-ort ходит на проверку через туннель, остальной трафик не затрагивается. Провал туннеля/блокировка → `verified:false, TELEGRAM_PAGE_NOT_FOUND_OR_OFFLINE` (best-effort). Фронт: `StepChannels` + `ChannelConnectModal` (реальный фронт) — ввод @хэндла/ссылки → verify → «Подключено» только при `verified`; хэндл кладётся в `WizardInput.channelHandles`. **Известное ограничение:** без туннеля telegram-verify неработоспособен напрямую из RU-сети — `t.me` и `api.telegram.org` недоступны (TCP timeout/HTTP 000; с rinhbaby недоступны и прямые IP Telegram `149.154.x.x`, и старый публичный прокси `194.67.95.7:3127`, IPv6 нет — проверено 2026-09-22). Делегировать проверку в AI-контур нельзя: развёрнутый Шлюз v2.5.0 не имеет ни эндпоинта, ни `task_type` для проверки канала (`quick_scan`/`collectors/analyze-brand` с `urls=[t.me/...]` возвращают пустые `raw_sources`), а его глубокий парсер `POST /api/v1/ai/website/analyze` с `url=t.me/UcustAi|t.me/s/UcustAi` отвечает `400 «Не удалось распарсить сайт»` (обычные сайты, напр. example.com, парсит на ура → интернет контур видит, а t.me — нет, т.к. телеграм недоступен/не отдаётся и с AI-ноды). Пока telegram-канал к проекту не привязать через UI-онбординг; обходно — `PATCH /projects/{id}` `socialLinks.telegram` (используется как контекст генерации и в RAG). **НО контур-нода МОЖЕТ проверять канал** (проверено 2026-09-10): на ней валидный `TELEGRAM_BOT_TOKEN` (`UcustAiPublisher_bot`) и `api.telegram.org` доступен (через прокси `194.67.95.7:3127`) → `getChat(@UcustAi)` возвращает `ok` (канал «UCust — ИИ маркетолог»). Т.е. делегировать verify контуру (новый task_type/эндпоинт типа `verify_channel` на Bot API) проще, чем парсить t.me. Telethon в venv ноды установлен, есть сессии (`ucust_session`/`ucust_userbot_session`) и TELETHON_PROXY_* в .env, но MTProto-подключение к DC через прокси падает (timeout) — без сетевой настройки telethon-парсер не поднять.
- Экспозиция через gateway: `POST /api/v0/orchestration/generate/async` (202), `GET /api/v0/orchestration/tasks/{taskId}`, `POST /api/v0/orchestration/ai/execute`, `POST /api/v0/orchestration/generate`, `POST /api/v0/orchestration/posts/generate` (multipart), `POST /api/v0/orchestration/analysis` + `GET /api/v0/orchestration/analysis/{id}`, `POST /api/v0/orchestration/socials/verify`, `POST /api/v1/ai/**` (callback, public). **Прод-фикс 2026-09-22**: в `api-gateway/application-prod.yml` добавлены `billing-service.uri` (`http://billing-service:8106`) и `generative-orchestration-service.uri` (`http://generative-orchestration-service:8107`) — в dev-yml они `localhost`, без прод-оверрайда `/api/v0/orchestration/**` падал 500 `Connection refused: localhost:8107`.
- **Анализ Telegram-канала (2026-09-22)**: задача из «Планируется» реализована. `POST /orchestration/channel/analyze` (body `{projectId, channel, limit, includeMediaVqa}`) → `ChannelAnalysisService.start` — **гейт**: проект ищется в business-service (`BusinessServiceClient.findProject`, ownership), у проекта должен быть привязан `socialLinks.telegram`, а `channel` запроса обязан совпадать с нормализованным привязанным (`ChannelNotBoundException`, 400 `CHANNEL_NOT_BOUND`, иначе UI/бэк (в т.ч. автоскан) не запустят анализ). `parse_telegram` через Шлюз, sync-backend, результат идемпотентно импортируется (`importPosts`, ключ `externalId`): посты PUBLISHED с `publishedAt`=date канала и **`targetPlatforms=telegram`** (раньше площадка не проставлялась — пост висел без метки в контент-плане), метрики → `post_statistics` (impressions←views, shares←forwards, comments←commentsCount). Возражения → RAG `channel_context:{projectId}`. **Автосканирование per-project**: `POST /orchestration/channel/settings` (GET) / `PUT /orchestration/channel/settings?projectId=...{autoRescanIntervalHours 1..168}` → сущность `project_channel_settings(project_id PK, interval_hours, last_rescan_at)` (таблица создаётся `ddl-auto: update`). Планировщик `ChannelAnalysisScheduler` переписан: тик каждые `services.channel-rescan.tick-ms` (default 900000 мс, было `interval-ms:21600000` для всех) пересканирует только пары, у которых истёк интервал из настроек (базис = `lastRescanAt`, либо время последнего COMPLETED-анализа); интервал настраивается пользователем в UI. Гейт привязок защищает от скана «чужих» каналов и после удаления TG из проекта. Фронт `ChannelAnalysisView`: ввод канала заблокирован (readOnly, берётся из привязанного socialLinks.telegram), без привязки — заглушка «Анализ станет доступен после привязки Telegram»; select «Авто-пересканирование» (1/3/6/12/24/48 ч) + «последний скан». Тесты: `ChannelAnalysisServiceTest` (гейт 3 кейса, targetPlatforms 3 кейса, настройки 3 кейса), фронт `modules.test.ts` (analyze/settings GET+PUT), контракт `docs/api/api-endpoints-public.json` пополнен GET/PUT `/orchestration/channel/settings`. **Проверено 2026-09-10 (актуально):** в развёрнутом Шлюзе v2.5.0 парсера НЕТ — `collect_channel`/`parse_channel`/`analyze_channel`/`telegram_collect` отвечают `Неизвестная задача`, parser-портов на ноде нет (только :8000; telethon+сессии установлены, MTProto через прокси 194.67.95.7:3127 не коннектится — timeout), поэтому E2E идёт только через мок (task `parse_telegram` → `get_mock_channel_result`). Ожидаемая форма ответа парсера (из спеки): `{source:"telethon", payload:{channel, limit, fetched_at, mode:"real"/"mock", messages:[{id, text, date, views, forwards, comments_count, media_type, media_path, comments:[{id, author, text, likes}], top_objections_from_comments:[...]}]}}` — маппер также понимает flat-форму `extracted_posts[]` + `subscribers` + `avg_views_per_post`.

## Local Development

```bash
# Infra only (services run in IntelliJ)
docker compose -f docker-compose.local.yml up postgres-db rabbitmq minio minio-init -d

# Full stack in Docker
docker compose -f docker-compose.local.yml up -d
```

## Ports

| Service | Internal | External |
|---------|----------|----------|
| PostgreSQL | 5432 | 5440 |
| RabbitMQ AMQP | 5672 | 5680 |
| RabbitMQ UI | 15672 | 15673 |
| MinIO API | 9000 | 9020 |
| MinIO Console | 9001 | 9021 |
| API Gateway | — | 8100 |
| Security Service | — | 8101 |
| User Service | — | 8102 |
| Notification Service | — | 8103 |
| Business Service | — | 8104 |
| Billing Service | — | 8106 |
| Generative Orchestration | — | 8107 |

## RabbitMQ Topology

```
Exchange: user-exchange (topic)
  → mail-notification-queue: user.registration.email, user.password.reset, user.account.locked, user.login.email (Уведомление о входе: email, IP, user-agent, город по IP)
  → mail-email-change-init-queue: user.email.change.init
  → mail-email-change-new-queue: user.email.change.new
  → mail-email-change-done-queue: user.email.change.done
  → user-profile-queue: user.created
  → user-profile-email-change-queue: user.email.change.done (обновляет email в user-profile)
  → user-profile-phone-backfill-queue: user.phone.backfill (security-service при входе существующего юзера через OAuth-провайдера публикует телефон из провайдера; user-service заполняет `user_profile.phone` только если он пуст)
  → project-generation-queue: project.created (generative-orchestration: async-генерация постов при создании проекта, см. ProjectEventHandler)
  → project-deletion-queue: project.deleted (generative-orchestration: каскадное удаление проекта — ProjectDeletedEventHandler → ProjectDeletionService: посты/задачи генерации/анализы БД + медиа MinIO `media/{projectId}/...`)
```

**Конвертер сообщений:** издатели обязаны слать JSON — `RabbitTemplate` с `JacksonJsonMessageConverter` (контент-тип `application/json`). Примечание 2026-09-10: business-service публиковал `project.created` дефолтным `SimpleMessageConverter` (Java-сериализация, `application/x-java-serialized-object`, body `[serialized object]`) → слушатель `project-generation-queue` падал с `Could not convert ... [serialized object]` / `No listener method found for class [B` и после ретраев уводил сообщение в DLQ. Фикс: добавлен `business-service/.../config/RabbitConfig.java` (JacksonJsonMessageConverter c `trustedPackages("*")` + RabbitTemplate, по образцу security-service).

## MinIO Buckets

- `user-service` (used by user-service and business-service)

## CI/CD Workflows

1. **ucust-dev** (`.github/workflows/main.yml`): push → build+push 7 images → SSH deploy to `~/reshala-project/`
2. **ucust-ops** (`ucust-ops/.github/workflows/deploy.yml`): push infra configs → SCP → SSH restart

## Needed Secrets (GitHub)

### ucust-dev repo
- `DB_PASSWORD`
- `JWT_SECRET_ACCESS`
- `GEO_API_URL` (geo-сервис для claim `city`, напр. `http://ip-api.com/json/{ip}?lang=ru&fields=status,country,city`; prod-строгий)
- `SERVER_TAILSCALE_IP`
- `AI_SERVICE_URI` (external generative AI endpoint)
- `AI_ORCHESTRATOR_ENDPOINT` (Единый Шлюз Оркестратора, напр. `http://10.0.0.2:8000`)
- `INTERNAL_API_SECRET` (общий секрет для заголовка `X-Internal-Secret`; бэкенд и AI-контур)
- `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `MAIL_SSL`, `MAIL_FROM` (SMTP; if unset — Mailtrap defaults, MAIL_SSL=false, MAIL_FROM=MAIL_USERNAME)

### ucust-dev repo (server .env, not GitHub)
- `QUOTA_FREE_UNLIMITED` — если `true`, FREE-тариф в billing становится безлимитным (posts/projects/aiGenerations=-1) идемпотентно при каждом старте. Docker-дефолт `false`, локальный dev-профиль — `true`. Для презентации задать `QUOTA_FREE_UNLIMITED=true` в .env на демо-сервере.
- `MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY` — scoped-юзер gen-ort для записи медиа в MinIO (`generative-orchestration-user` / `gen-strong-pass`, создаётся minio-init). `MINIO_ENDPOINT` в проде с дефолтом `http://minio:9000`. `MINIO_PUBLIC_BASE_URL` — публичный URL до бакета через gateway-роут `/s3/**`: `https://api.ucust.n4d3sh1k4.site/s3/generative-orchestration` (должен быть доступен AI-контуру на 10.0.0.2). Все строгие.
- `USER_MINIO_ACCESS_KEY`/`USER_MINIO_SECRET_KEY` (опционально, дефолт admin/password123) — креды user-service для аватарок; рекомендуется заменить на scoped `user-service-user`/`usr-strong-pass` из minio-init.
- `MINIO_S3_URI` (опционально, дефолт `http://minio:9000`) — куда gateway проксирует публичный `/s3/**` на MinIO (в docker prod `http://minio:9000`, НЕ `localhost:9020`).
- `TELEGRAM_BOT_TOKEN` (опционально) — токен бота (`UcustAiPublisher_bot`) для верификации TG через Bot API `getChat`. `TELEGRAM_BOT_PROXY` (опционально, дефолт `http://xray:1080`) — HTTP-пул (покупные прокси, разделитель запятая/перевод строки): `host:port` | `http://host:port` | `user:pass@host:port` | `host:port:user:pass`, ротация round-robin; в проде обычно локальный xray-туннель (сервис `xray` в compose → VLESS MagnumVPN → `http://xray:1080`); нужен, когда Telegram/соцсети недоступны напрямую из сети сервера (RU-блокировки). Без него верификация из RU-сети почти всегда вернёт `verified:false`.

### ucust-ops repo
- `POSTGRES_PASSWORD`
- `RABBITMQ_PASS`
- `MINIO_ROOT_PASSWORD`
- `SERVER_TAILSCALE_IP`, `SERVER_USER`, `SERVER_SSH_KEY`
- `TS_OAUTH_CLIENT_ID`, `TS_OAUTH_SECRET`
